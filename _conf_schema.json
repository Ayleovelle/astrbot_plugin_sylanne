{
  "sylanne_persona_name": {
    "description": "人格名称（显示在 WebUI 和日志中）",
    "type": "string",
    "default": "Sylanne",
    "hint": "填写当前会话中使用的人格/角色名字。"
  },
  "sylanne_enable_v2core": {
    "description": "v2core 认知内核（唯一认知核；关闭=紧急回退 v1 旧认知）",
    "type": "bool",
    "default": true,
    "hint": "默认开。v2core 双层 agent（PERCEPT@请求注入心象/评价 + DELIBERATE/EVOLVE@回复裁决学习）是唯一认知内核；启用即退役 v1 逐轮认知（旧 9-agent SelfCore PRE/POST/RESPONSE_POST 与逐轮 LLM 评估不再运行，省每轮评估开销）。存档无损复用同一引擎；任何异常单轮回落旧管线不阻断回复。置 false 仅作部署级紧急回退。"
  },
  "sylanne_no_ghost_reply": {
    "description": "空回复兜底（按成因分治）",
    "type": "bool",
    "default": true,
    "hint": "默认开。空回复按成因分治：模型把答案塞进 thinking 块（strip 后空）→ 兜底一句防已读不回；工具循环死锁等真空回复 → 静默不出声（避免蹦出解释式怪话）。若你在下方填了自定义兜底语，则真空回复也走你的兜底。关闭本项=所有空回复都静默（恢复旧行为，不建议）。"
  },
  "sylanne_ghost_fallback_text": {
    "description": "空回复兜底语（自定义）",
    "type": "string",
    "default": "",
    "hint": "留空=按成因分治（thinking 包答案才兜底，工具死锁静默）。填了则任何空回复都用这句兜底——建议用她自己的口吻（如一句短停顿/岔话），别写成第三人称旁白。"
  },
  "sylanne_webui_enabled": {
    "description": "启用 WebUI 管理面板",
    "type": "bool",
    "default": false,
    "hint": "启用后可通过浏览器访问记忆管理面板。"
  },
  "sylanne_group_attention_trigger_names": {
    "description": "群聊触发名字（逗号分隔）",
    "type": "string",
    "default": "",
    "hint": "除人格名字外，额外的触发关键词。如：小希,希希,Sylanne"
  },
  "sylanne_webui_host": {
    "description": "WebUI 监听地址",
    "type": "string",
    "default": "127.0.0.1",
    "hint": "127.0.0.1 仅本机访问；0.0.0.0 允许局域网访问。"
  },
  "sylanne_webui_port": {
    "description": "WebUI 监听端口",
    "type": "int",
    "default": 2718,
    "hint": "WebUI 服务端口，确保不与 AstrBot 主端口冲突。"
  },
  "sylanne_webui_token": {
    "description": "WebUI 访问令牌（留空自动生成）",
    "type": "string",
    "default": "",
    "hint": "首次启动自动生成。复制此 token 到浏览器访问 WebUI。"
  },
  "sylanne_alpha_root": {
    "description": "Sylanne alpha body 状态目录（留空则使用 AstrBot data 目录下的 sylanne_alpha/）",
    "type": "string",
    "default": "",
    "hint": "每个会话会保存为独立的 .alpha.json；留空自动使用 AstrBot data/sylanne_alpha/ 目录。覆盖安装前请备份旧 Sylanne 数据。"
  },
  "sylanne_alpha_realtime_chat_enabled": {
    "description": "启用 Sylanne 即时聊天调度",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后仍受用户主权、风险、冷却和预算边界限制。"
  },
  "sylanne_alpha_stream_first_sentence_enabled": {
    "description": "启用首句抢发（流式）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。开启后在 LLM 流式生成过程中检测到第一个完整句子就提前发给用户，降低感知延迟。需要 LLM Provider 开启流式输出。"
  },
  "sylanne_alpha_realtime_intercept_llm_response": {
    "description": "允许即时聊天接管 LLM 响应分段",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；用于对话分段和打断识别，不恢复旧 realtime engine。"
  },
  "sylanne_alpha_proactive_dispatch_enabled": {
    "description": "启用 Sylanne 主动发起派发",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后 guard 不允许时仍不会派发。"
  },
  "sylanne_alpha_proactive_scheduler_enabled": {
    "description": "启用主动检查后台调度",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；用于低频检查，不执行高频循环。"
  },
  "sylanne_alpha_proactive_bridge_enabled": {
    "description": "启用主动发言桥接（交给 astrbot_plugin_proactive_chat 发送）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后 Sylanne 决定何时主动+提供生活素材，由大饼主动消息插件负责发送。需已安装 astrbot_plugin_proactive_chat，未安装时静默降级。"
  },
  "sylanne_alpha_proactive_segment_takeover": {
    "description": "由 Sylanne 接管大饼主动消息的分段发送",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后关掉大饼自己的分段，改由 Sylanne 用人格化节奏把主动消息拆成多条连发（更像真人）。仅在桥接开启时生效。"
  },
  "sylanne_alpha_proactive_hesitation": {
    "description": "主动发言的犹豫感（发前迟疑/最后一刻收回/踌躇词）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后主动发言会带人类式的迟疑：发出前随机停顿、按犹豫强度可能最后一刻收回不发、偶尔先发个踌躇词起头。强度由 Sylanne 的内在状态（怕打扰/想安静/熟悉度）驱动。仅在桥接开启时生效。"
  },
  "sylanne_alpha_recall_mode": {
    "description": "记忆召回引擎模式",
    "type": "string",
    "default": "legacy",
    "options": ["legacy", "shadow", "activation"],
    "hint": "legacy=现有四维加权（默认）；shadow=新旧并跑只记差异不改行为（灰度评估用）；activation=启用 ACT-R 激活核召回。建议先 shadow 观察 /api/memory/recall_debug 的 overlap 再切 activation。"
  },
  "sylanne_alpha_embedding_memory_enabled": {
    "description": "启用 embedding 记忆辅助召回",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后需要配置 embedding provider。"
  },
  "sylanne_alpha_embedding_memory_provider_id": {
    "description": "Embedding 记忆使用的 Provider ID",
    "type": "string",
    "default": "",
    "hint": "填写 AstrBot 中配置的 embedding provider ID；留空则不启用向量召回。"
  },
  "sylanne_alpha_embedding_memory_top_k": {
    "description": "Embedding 召回 top-K 条数",
    "type": "int",
    "default": 3,
    "hint": "每次召回最多返回的相似记忆条数。"
  },
  "sylanne_alpha_fast_assessor_enabled": {
    "description": "启用即时判断 LLM",
    "type": "bool",
    "default": true,
    "hint": "默认开启；前台同步等待（2s），即时情绪直接注入当前轮回复。"
  },
  "sylanne_alpha_fast_assessor_provider_id": {
    "description": "即时判断 LLM Provider（前台同步）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "前台同步等待 2s，需要响应快的小模型。即时情绪直接影响当前轮回复语气。推荐设置：温度 0，top-p 0.1，Max Tokens 无需设置（插件已接管）。"
  },
  "sylanne_alpha_background_workers_enabled": {
    "description": "启用后台工作队列",
    "type": "bool",
    "default": true,
    "hint": "默认开启；用于异步持久化和后台任务。"
  },
  "sylanne_alpha_background_max_workers": {
    "description": "后台最大并发工作数",
    "type": "int",
    "default": 2,
    "hint": "1c1g 环境建议保持 1-2。"
  },
  "sylanne_alpha_life_simulation_enabled": {
    "description": "启用 Sylanne 生活模拟",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后 Sylanne 会用外部 LLM 模拟自己的生活，某些时刻会因为她那边发生的事主动找用户。"
  },
  "sylanne_alpha_life_simulation_use_custom_persona": {
    "description": "生活模拟使用自定义人设",
    "type": "bool",
    "default": false,
    "hint": "默认关闭时自动读取 AstrBot 角色人设；开启后使用下方自定义文本覆盖，适用于想让生活中的角色和对话中的角色有差异的场景。"
  },
  "sylanne_alpha_life_simulation_custom_persona": {
    "description": "生活模拟自定义人设文本",
    "type": "string",
    "default": "",
    "hint": "仅在上方开关开启时生效。为生命模拟单独指定一段人设描述，覆盖 AstrBot 默认人设。最多 500 字。"
  },
  "sylanne_alpha_life_simulation_provider_id": {
    "description": "生活模拟 LLM Provider",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "用于模拟 Sylanne 生活的 LLM；建议用便宜的小模型。启用生活模拟时必填，留空会静默失效（生活状态冻结、主动消息可能复读）。"
  },
  "sylanne_alpha_life_simulation_interval_seconds": {
    "description": "生活模拟间隔（秒）",
    "type": "float",
    "default": 1800.0,
    "hint": "每隔多久模拟一次 Sylanne 的生活状态。默认 30 分钟。"
  },
  "sylanne_alpha_life_simulation_outreach_cooldown_seconds": {
    "description": "生活模拟主动找用户冷却（秒）",
    "type": "float",
    "default": 3600.0,
    "hint": "因为生活事件主动找用户的最短间隔。默认 1 小时。"
  },
  "sylanne_alpha_life_simulation_share_intensity": {
    "description": "Sylanne 主动分享强度（off=关闭主动分享 / low=低频 / standard=标准 / high=高频）",
    "type": "string",
    "default": "standard",
    "options": ["off", "low", "standard", "high"],
    "hint": "Phase 4：生活观测面板的总闸。off=完全关闭主动分享（但仍模拟生活）；low/standard/high 按节律调整门控宽严。仅控制生活模拟分享侧，不影响普通会话回复。"
  },
  "sylanne_alpha_life_simulation_night_consolidation": {
    "description": "夜间/低活跃期执行生活反思巩固",
    "type": "bool",
    "default": true,
    "hint": "Phase 2/3：在夜间或低活跃期跑反思 + 巩固，沉淀次日叙事弧和项目进度。关闭会让 plan/projects 停留在最近一次模拟产出，不再被反思修正。"
  },
  "sylanne_alpha_life_simulation_allow_memory_write": {
    "description": "允许生活模拟事件写入长期记忆",
    "type": "bool",
    "default": true,
    "hint": "Phase 3：开启时高重要度的生活事件可作为 user_fact/common_ground 候选写入长期记忆。关闭则生活完全停留在生活模拟侧不污染主记忆。"
  },
  "sylanne_alpha_auto_detect_group_context": {
    "description": "自动检测 AstrBot 群聊上下文感知",
    "type": "bool",
    "default": true,
    "hint": "默认开启。当 AstrBot 自带的群聊上下文感知功能已启用时，自动跳过 Sylanne 的 shadow buffer 注入以避免重复上下文。社交场计算不受影响。"
  },
  "sylanne_alpha_hajide_compat_mode": {
    "description": "哈基德模型兼容（影响 prompt 构建和响应处理）",
    "type": "bool",
    "default": false,
    "hint": "仅在使用 Hajide 前端时开启；会调整 system prompt 格式和响应后处理逻辑。"
  },
  "sylanne_alpha_main_assessor_enabled": {
    "description": "启用主评估器（深度情感分析，后台异步）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后情绪估计使用独立 provider，不占用主聊天模型。后台深度语义分析结果喂给长期状态引擎。"
  },
  "sylanne_alpha_main_assessor_provider_id": {
    "description": "主评估器 LLM Provider ID（后台异步）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "后台深度语义分析，不阻塞回复，可用强模型。推荐设置：温度 0，top-p 0.1。"
  },
  "sylanne_alpha_locked_persona_prompt": {
    "description": "锁定的人格 system prompt（覆盖动态生成）",
    "type": "string",
    "default": "",
    "hint": "填写后将覆盖 Sylanne 动态生成的 system prompt；留空则使用人格引擎自动生成。适用于调试或固定人设场景。"
  },
  "sylanne_alpha_transcription_enabled": {
    "description": "启用非文本消息转述（图片→文字描述，自动检测多模态 provider）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭；开启后收到图片/语音等非文本消息时，自动调用多模态 LLM 转述为文字描述供 Sylanne 理解。"
  },
  "sylanne_alpha_transcription_provider_id": {
    "description": "转述 LLM 的 provider ID（留空自动检测多模态 provider）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "指定用于转述的多模态 LLM provider；留空则自动检测已配置的多模态 provider。"
  },
  "sylanne_alpha_owner_id": {
    "description": "主人身份 ID（sender_id）：仅此身份的会话可被自动判定为亲密关系，/bond 也仅此身份可用",
    "type": "string",
    "default": "",
    "hint": "Phase 2B 防对抗性注入：填你的平台 sender_id（如 QQ 号）。留空则全部会话禁用自动亲密晋升、/bond 拒绝（fail-closed）。内容信号可伪造，只有身份不可伪造。"
  },
  "sylanne_alpha_rel_register_provider_id": {
    "description": "关系类型分类 LLM 的 provider ID（留空回退 assessor/emotion provider）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "Phase 2B：低频后台判会话关系语域（恋人/朋友/同事）。留空则复用 assessor provider。"
  }
}
