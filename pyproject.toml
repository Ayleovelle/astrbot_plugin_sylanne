[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
# CI 只检查致命问题：语法错误、未定义变量、重复 import
select = ["E9", "F63", "F7", "F82"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"experiments/*" = ["ALL"]
"theory/*" = ["ALL"]
